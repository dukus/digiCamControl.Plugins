<UserControl x:Class="Macrophotography.MacroPluginControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Core="clr-namespace:CameraControl.Core;assembly=CameraControl.Core"
             xmlns:translation="clr-namespace:CameraControl.Core.Translation;assembly=CameraControl.Core">
    <Grid>
        <Button Height="50" Name="but_qr" Width="50" Margin="5" Style="{DynamicResource MetroCircleButtonStyle}"  ToolTip="Macrophotography" Click="but_qr_Click">
            <Rectangle Width="25" Height="25">
                <Rectangle.Fill>
                    <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_camera_auto}" />
                </Rectangle.Fill>
            </Rectangle>
        </Button>
    </Grid>
</UserControl>
